import { createContext } from 'react';

export const SnackBarContext = createContext({});
export const SearchBarExpenditureContext = createContext({});
export const IsLoadingContext = createContext({});