import LoginForm from 'components/login/LoginForm';
import Grid from '@mui/material/Grid';

const LoginPage = () => {
    return (
        <div> 
            <LoginForm/>
        </div>
    )
}

export default LoginPage

// style={{ backgroundImage: `url(/images/backgroundImageForLoginPage.png)`, backgroundSize: `100vw 100vh`, backgroundRepeat: `no-repeat`}}