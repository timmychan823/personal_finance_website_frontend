import { FC, Fragment } from 'react';

const LandingPage: FC = () => { //TODO: use sub in decoded access token to define what data it can see
    return (
        <Fragment>
            <h1>Landing Page</h1>
        </Fragment>

    )
}

export default LandingPage